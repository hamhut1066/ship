(ns ship-test
  (:require [clojure.test :refer :all]
            [ship :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest tests-work
(testing "something here"
(is (= 0 1))))
